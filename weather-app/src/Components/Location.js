import React from 'react';

const Location = ({ city } ) => {
   // Destructuring
   return (
           <div>
              <h1>{city}</h1>
            </div>
          );
};

export default Location;